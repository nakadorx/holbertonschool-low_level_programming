#include "holberton.h"
/**
* rev_string - check the code for Holberton School students.
* @a : char
* @n : char
* Return: Always 0.
*/

void reverse_array(int *a, int n)
{
int  x;
int m[509];
for (x = n  ; x >= 0  ; x--)
{
a[n - x] = m[x];
}
}
